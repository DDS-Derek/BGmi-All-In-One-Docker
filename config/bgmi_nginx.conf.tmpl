server {
    listen 80 default_server;
    server_name _;
    root /bgmi/;
    autoindex on;
    charset utf-8;

    location /bangumi {
        alias {{ .Env.DOWNLOAD_DIR }};
    }
{{ .Env.NGINX_PARAMETER }}
}